<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Auth.Connexion.View.XF_SignUpPage">



    <StackLayout>
        <StackLayout Margin="20" VerticalOptions="CenterAndExpand" Spacing="20" Orientation="Vertical">

            <Entry Placeholder="Pseudo" x:Name="Pseudo" Text="{Binding Pseudo}"></Entry>

            <Entry x:Name="Emailentery" Placeholder="Email" Text="{Binding Email}" Keyboard="Email"    
                 />

            <Entry x:Name="passwordentery" Placeholder="Password" Text="{Binding Password}"     
                   IsPassword="True"/>

            <Entry x:Name="cfmpasswordentery" Placeholder="Re_Enter Password" Text="{Binding ConfirmPassword}"     
                   IsPassword="True" />
            <Label Text="Etes vous Premium : " FontSize="18">

            </Label>
            <CheckBox x:Name="ispremiumentry" IsChecked="True" Color="#2097F4"
                      />



            <Button x:Name="signup" Text="SignUp" Command="{Binding SignUpCommand}" HorizontalOptions="Center" BackgroundColor="#2097F4"
                     TextColor="White"/>

        </StackLayout>
    </StackLayout>

</ContentPage>